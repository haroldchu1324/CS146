We first compare the length of two stings. If they are not the same we return false. Then we convert both
strings to lower case. We create a character array and put in the two strings in each array. Then we sort
and compare the array. If they are equal we return true, else false.