We first sort the array nums. We will be skipping duplicates by checking and continue if it is. This is
to not add the duplicate triplets to the result. We use two pointer to find pairs that sum to the target
value. If the triplet adds to zero we add it to the result list.